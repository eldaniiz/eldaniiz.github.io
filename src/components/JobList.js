import React from "react";
import PropTypes from "prop-types";
import { makeStyles } from "@material-ui/core/styles";
import Tabs from "@material-ui/core/Tabs";
import Tab from "@material-ui/core/Tab";
import Typography from "@material-ui/core/Typography";
import Box from "@material-ui/core/Box";
import FadeInSection from "./FadeInSection";

const isHorizontal = window.innerWidth < 600;

function TabPanel(props) {
  const { children, value, index, ...other } = props;

  if (isHorizontal) {
    return (
      <div
        role="tabpanel"
        hidden={value !== index}
        id={`full-width-tabpanel-${index}`}
        aria-labelledby={`full-width-tab-${index}`}
        {...other}
      >
        {value === index && (
          <Box p={3}>
            <Typography>{children}</Typography>
          </Box>
        )}
      </div>
    );
  } else {
    return (
      <div
        role="tabpanel"
        hidden={value !== index}
        id={`vertical-tabpanel`}
        {...other}
      >
        {value === index && (
          <Box p={3}>
            <Typography>{children}</Typography>
          </Box>
        )}
      </div>
    );
  }
}

TabPanel.propTypes = {
  children: PropTypes.node,
  index: PropTypes.any.isRequired,
  value: PropTypes.any.isRequired
};

function a11yProps(index) {
  if (isHorizontal) {
    return {
      id: `full-width-tab-${index}`,
      "aria-controls": `full-width-tabpanel-${index}`
    };
  } else {
    return {
      id: `vertical-tab-${index}`
    };
  }
}

const useStyles = makeStyles(theme => ({
  root: {
    flexGrow: 1,
    backgroundColor: "theme.palette.background.paper",
    display: "flex",
    height: 300
  },
  tabs: {
    borderRight: `1px solid ${theme.palette.divider}`
  }
}));

const JobList = () => {
  const classes = useStyles();
  const [value, setValue] = React.useState(0);

  const experienceItems = {
    DillBill: {
      jobTitle: "Software Engineer @",
      duration: "APR 2021 - PRESENT",
      desc: [
        "Working experience with PHP's Yii2 framework",
        "Experience with Stripe API(Payment System)",
        "Developed structured CRM Management system works with Google Sheets API",
        "Tech Stack: PHP, HTML/CSS, JavaScript, Python"
      ]
    },
    VRiendly: {
      jobTitle: "Jr. Software Engineer @",
      duration: "DEC 2019 - DEC 2020",
      desc: [
        "Developed APIs for load data which comes from web application into VR application",
        "Tech Stack: Python, Git, Tensorflow, Django"
      ]
    },
    Whelp: {
      jobTitle: "Deep Learning Intern(NLP) @",
      duration: "SEPT 2019 - DEC 2019",
      desc: [
        "Experience with Natural Language Understanding Systems",
        "Learned Deep Learning in Natural Language Processing",
        "Developed Intent Classification, Slot Filling, and Open Domain Question Answering",
        "Tech Stack: Python, Git, Linux, Jira, Tensorflow, Keras"
      ]
    },
    "Keepface": {
      jobTitle: "Deep Learning Intern(Computer Vision) @",
      duration: "MAY 2019 - AUG 2019",
      desc: [
        "Develped Gender Classification Service",
        "Experience With Computer Vision Algorithms",
        "Tech Stack: Flask, OpenCV, Python\n"
      ]
    }
  };

  const handleChange = (event, newValue) => {
    setValue(newValue);
  };

  return (
    <div className={classes.root}>
      <Tabs
        orientation={!isHorizontal ? "vertical" : null}
        variant={isHorizontal ? "fullWidth" : "scrollable"}
        value={value}
        onChange={handleChange}
        className={classes.tabs}
      >
        {Object.keys(experienceItems).map((key, i) => (
          <Tab label={isHorizontal ? `0${i}.` : key} {...a11yProps(i)} />
        ))}
      </Tabs>
      {Object.keys(experienceItems).map((key, i) => (
        <TabPanel value={value} index={i}>
          <span className="joblist-job-title">
            {experienceItems[key]["jobTitle"] + " "}
          </span>
          <span className="joblist-job-company">{key}</span>
          <div className="joblist-duration">
            {experienceItems[key]["duration"]}
          </div>
          <ul className="job-description">
            {experienceItems[key]["desc"].map(function(descItem, i) {
              return (
                <FadeInSection delay={`${i + 1}00ms`}>
                  <li key={i}>{descItem}</li>
                </FadeInSection>
              );
            })}
          </ul>
        </TabPanel>
      ))}
    </div>
  );
};

export default JobList;
